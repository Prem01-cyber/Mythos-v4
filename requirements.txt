# Install Unsloth first — it pins compatible torch/transformers versions.
# On the cloud machine run:
#   pip install unsloth
#   pip install -r requirements.txt

# Core training stack
transformers>=4.45.0
datasets>=2.20.0
trl>=0.12.0
peft>=0.13.0
accelerate>=0.34.0
bitsandbytes>=0.44.0

# Inference / eval
# vLLM must be installed separately — it has its own CUDA build.
# On the cloud machine run:
#   pip install vllm
sentencepiece
protobuf

# Data pipeline (source1_exploitdb.py)
openai>=1.40.0
pandas>=2.0.0
tiktoken>=0.7.0
tqdm>=4.65.0
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
